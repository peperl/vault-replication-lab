# Kind cluster config for the External Secrets Operator test environment.
# This cluster is separate from the Vault cluster and is used to deploy
# the ESO operator and associated workloads.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: vault-lab-eso
nodes:
  - role: control-plane
  - role: worker
  - role: worker
